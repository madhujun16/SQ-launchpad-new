# Backend API Configuration
# Copy this to .env and update with your backend URL

# Option 1: Using IP address (for local/development backend)
VITE_API_BASE_URL=http://12.12.121.2:8080/api

# Option 2: Using domain (for production)
# VITE_API_BASE_URL=https://sqlaunchpad.com/api

# Option 3: Local backend
# VITE_API_BASE_URL=http://localhost:3000/api

# Development Mode
# Set to 'false' to use real backend API (requires working backend)
# Set to 'true' to use mock authentication (for local testing)
VITE_USE_MOCK_AUTH=true

# Note: The sqlaunchpad-backend.json service account file is for
# backend server authentication with GCP, not for frontend use.
# Your backend server uses it to access GCP services.
